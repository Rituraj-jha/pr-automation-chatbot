# Markdown LLM Test Input

Replace this file with the Markdown content you want to send directly to the TrueFoundry-backed LLM.

## Example request

Given this Markdown, explain what resource fields can be derived and what inputs are still missing.